import { id, timestamp } from "./model.mjs";
export function createApproval(input, { now = () => Date.now() } = {}) { if (input.actor?.type !== "human" || !input.actor.id || input.explicitConfirmation !== true) throw new Error("Only an authenticated human actor may create a lifecycle approval with explicit confirmation."); return { schemaVersion: 1, id: id("APR"), projectId: input.projectId, gate: input.gate, decision: "approved", actor: input.actor, explicitConfirmation: true, lifecycleVersion: input.lifecycleVersion, bindings: input.bindings, createdAt: timestamp(now), ...(input.expiresAt ? { expiresAt: input.expiresAt } : {}) }; }
export function approvalIsCurrent(approval, invalidated, bindings, now = Date.now()) { return Boolean(approval && approval.decision === "approved" && !invalidated && (!approval.expiresAt || Date.parse(approval.expiresAt) > now) && Object.entries(bindings).every(([key, value]) => JSON.stringify(approval.bindings[key]) === JSON.stringify(value))); }
