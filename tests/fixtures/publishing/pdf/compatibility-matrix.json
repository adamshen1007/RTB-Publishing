{
  "schemaVersion": 2,
  "fixture": "semantic-book.md",
  "purpose": "Run this exact canonical Markdown snapshot through the locked derived-Typst command on each release-producing platform before release use.",
  "expected": {
    "profiles": ["PDF/A-2a+PDF/UA-1", "2a", "ua1"],
    "language": "en-US",
    "semanticElements": ["heading", "list", "figure", "figure-alt", "table", "table-header", "internal-link", "external-link"],
    "metadata": ["title", "author", "keywords", "date"],
    "navigation": ["outline-bookmark", "internal-link", "external-link"],
    "fonts": ["Noto Serif only"]
  },
  "platforms": [
    {
      "id": "macos-x86_64",
      "status": "passed-2026-07-28",
      "rendererArtifact": "Typst 0.15.0 macOS x86_64 with pinned qpdf, Temurin, veraPDF, and Noto Serif",
      "command": ["node", "scripts/pdf-compatibility.mjs"],
      "evidence": "tests/fixtures/publishing/pdf/evidence/artifacts/manifest.json"
    }
  ],
  "ciBoundary": {
    "workflowLabel": "macos-15-intel",
    "architecture": "x86_64",
    "status": "implemented-pending-remote-run-evidence",
    "requiredBeforePromotion": "Run the identical node scripts/pdf-compatibility.mjs command in GitHub Actions and retain its verified manifest and reports."
  }
}
